# test_function.calc
# Test custom function definition

fn square(x) {
    return x * x;
}

fn factorial(n) {
    if (n <= 1) {
        return 1;
    }
    return n * factorial(n - 1);
}

square(5) + factorial(5);
