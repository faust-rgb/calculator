# Test Exact Mode and Persistence
print("--- Exact Mode Tests ---")
# This requires a way to turn it on from script
# But :exact is a REPL command. execute_script might not support it.
# Wait, evaluate_for_display takes exact_mode.
# execute_script takes exact_mode.
# There is no built-in function to toggle it mid-script.
# Let's see if arithmetic on fractions works if passed from outside.

print("1/3 + 1/4 = ", 1/3 + 1/4)

print("--- Persistence Tests ---")
x_saved = 42
m_saved = [1, 2; 3, 4]
# :save is a REPL command, but it might be handled in process_line.
# In a script, we can't easily call :save unless we use a hack.
# Actually, the user can run bin/calculator with a script.
# I'll test saving and loading via separate commands if possible.

print("Saving state to reval_state.txt...")
# This will likely fail if called as a normal statement
# :save reval_state.txt
